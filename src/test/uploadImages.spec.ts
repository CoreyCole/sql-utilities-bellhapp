// import { uploadImagesScripts } from '../src/uploadImages';

describe('upload images', () => {
  describe('search restaurant menu', () => {
    // it('should return the items from a restaurant menu like the given name', async () => {
    //   const items =
    // })
    it('should wor', () => {
      expect(true).toBeTruthy();
    });
  });
});
